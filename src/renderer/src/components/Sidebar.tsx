type SidebarProps = {};

export const Sidebar = (props: SidebarProps) => {
    const {} = props;
    return (
        <div className="p-2">
            <span>ZenPlayer</span>
        </div>
    );
};
